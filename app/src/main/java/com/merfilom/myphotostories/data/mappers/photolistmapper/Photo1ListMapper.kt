package com.merfilom.myphotostories.data.mappers.photolistmapper

import com.merfilom.myphotostories.data.mappers.PhotoMapperInterface
import com.merfilom.myphotostories.data.models.photomodels.Photo1Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo2Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo3Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo4Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo5Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo6Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo7Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo8Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo9Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo10Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo11Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo12Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo13Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo14Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo15Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo16Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo17Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo18Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo19Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo20Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo21Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo22Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo23Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo24Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo25Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo26Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo27Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo28Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo29Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo30Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo31Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo32Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo33Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo34Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo35Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo36Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo37Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo38Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo39Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo40Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo41Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo42Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo43Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo44Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo45Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo46Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo47Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo48Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo49Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo50Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo51Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo52Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo53Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo54Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo55Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo56Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo57Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo58Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo59Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo60Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo61Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo62Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo63Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo64Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo65Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo66Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo67Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo68Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo69Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo70Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo71Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo72Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo73Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo74Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo75Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo76Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo77Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo78Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo79Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo80Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo81Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo82Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo83Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo84Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo85Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo86Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo87Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo88Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo89Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo90Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo91Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo92Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo93Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo94Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo95Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo96Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo97Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo98Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo99Entity
import com.merfilom.myphotostories.data.models.photomodels.Photo100Entity
import com.merfilom.myphotostories.domain.models.photomodels.Photo1
import com.merfilom.myphotostories.domain.models.photomodels.Photo2
import com.merfilom.myphotostories.domain.models.photomodels.Photo3
import com.merfilom.myphotostories.domain.models.photomodels.Photo4
import com.merfilom.myphotostories.domain.models.photomodels.Photo5
import com.merfilom.myphotostories.domain.models.photomodels.Photo6
import com.merfilom.myphotostories.domain.models.photomodels.Photo7
import com.merfilom.myphotostories.domain.models.photomodels.Photo8
import com.merfilom.myphotostories.domain.models.photomodels.Photo9
import com.merfilom.myphotostories.domain.models.photomodels.Photo10
import com.merfilom.myphotostories.domain.models.photomodels.Photo11
import com.merfilom.myphotostories.domain.models.photomodels.Photo12
import com.merfilom.myphotostories.domain.models.photomodels.Photo13
import com.merfilom.myphotostories.domain.models.photomodels.Photo14
import com.merfilom.myphotostories.domain.models.photomodels.Photo15
import com.merfilom.myphotostories.domain.models.photomodels.Photo16
import com.merfilom.myphotostories.domain.models.photomodels.Photo17
import com.merfilom.myphotostories.domain.models.photomodels.Photo18
import com.merfilom.myphotostories.domain.models.photomodels.Photo19
import com.merfilom.myphotostories.domain.models.photomodels.Photo20
import com.merfilom.myphotostories.domain.models.photomodels.Photo21
import com.merfilom.myphotostories.domain.models.photomodels.Photo22
import com.merfilom.myphotostories.domain.models.photomodels.Photo23
import com.merfilom.myphotostories.domain.models.photomodels.Photo24
import com.merfilom.myphotostories.domain.models.photomodels.Photo25
import com.merfilom.myphotostories.domain.models.photomodels.Photo26
import com.merfilom.myphotostories.domain.models.photomodels.Photo27
import com.merfilom.myphotostories.domain.models.photomodels.Photo28
import com.merfilom.myphotostories.domain.models.photomodels.Photo29
import com.merfilom.myphotostories.domain.models.photomodels.Photo30
import com.merfilom.myphotostories.domain.models.photomodels.Photo31
import com.merfilom.myphotostories.domain.models.photomodels.Photo32
import com.merfilom.myphotostories.domain.models.photomodels.Photo33
import com.merfilom.myphotostories.domain.models.photomodels.Photo34
import com.merfilom.myphotostories.domain.models.photomodels.Photo35
import com.merfilom.myphotostories.domain.models.photomodels.Photo36
import com.merfilom.myphotostories.domain.models.photomodels.Photo37
import com.merfilom.myphotostories.domain.models.photomodels.Photo38
import com.merfilom.myphotostories.domain.models.photomodels.Photo39
import com.merfilom.myphotostories.domain.models.photomodels.Photo40
import com.merfilom.myphotostories.domain.models.photomodels.Photo41
import com.merfilom.myphotostories.domain.models.photomodels.Photo42
import com.merfilom.myphotostories.domain.models.photomodels.Photo43
import com.merfilom.myphotostories.domain.models.photomodels.Photo44
import com.merfilom.myphotostories.domain.models.photomodels.Photo45
import com.merfilom.myphotostories.domain.models.photomodels.Photo46
import com.merfilom.myphotostories.domain.models.photomodels.Photo47
import com.merfilom.myphotostories.domain.models.photomodels.Photo48
import com.merfilom.myphotostories.domain.models.photomodels.Photo49
import com.merfilom.myphotostories.domain.models.photomodels.Photo50
import com.merfilom.myphotostories.domain.models.photomodels.Photo51
import com.merfilom.myphotostories.domain.models.photomodels.Photo52
import com.merfilom.myphotostories.domain.models.photomodels.Photo53
import com.merfilom.myphotostories.domain.models.photomodels.Photo54
import com.merfilom.myphotostories.domain.models.photomodels.Photo55
import com.merfilom.myphotostories.domain.models.photomodels.Photo56
import com.merfilom.myphotostories.domain.models.photomodels.Photo57
import com.merfilom.myphotostories.domain.models.photomodels.Photo58
import com.merfilom.myphotostories.domain.models.photomodels.Photo59
import com.merfilom.myphotostories.domain.models.photomodels.Photo60
import com.merfilom.myphotostories.domain.models.photomodels.Photo61
import com.merfilom.myphotostories.domain.models.photomodels.Photo62
import com.merfilom.myphotostories.domain.models.photomodels.Photo63
import com.merfilom.myphotostories.domain.models.photomodels.Photo64
import com.merfilom.myphotostories.domain.models.photomodels.Photo65
import com.merfilom.myphotostories.domain.models.photomodels.Photo66
import com.merfilom.myphotostories.domain.models.photomodels.Photo67
import com.merfilom.myphotostories.domain.models.photomodels.Photo68
import com.merfilom.myphotostories.domain.models.photomodels.Photo69
import com.merfilom.myphotostories.domain.models.photomodels.Photo70
import com.merfilom.myphotostories.domain.models.photomodels.Photo71
import com.merfilom.myphotostories.domain.models.photomodels.Photo72
import com.merfilom.myphotostories.domain.models.photomodels.Photo73
import com.merfilom.myphotostories.domain.models.photomodels.Photo74
import com.merfilom.myphotostories.domain.models.photomodels.Photo75
import com.merfilom.myphotostories.domain.models.photomodels.Photo76
import com.merfilom.myphotostories.domain.models.photomodels.Photo77
import com.merfilom.myphotostories.domain.models.photomodels.Photo78
import com.merfilom.myphotostories.domain.models.photomodels.Photo79
import com.merfilom.myphotostories.domain.models.photomodels.Photo80
import com.merfilom.myphotostories.domain.models.photomodels.Photo81
import com.merfilom.myphotostories.domain.models.photomodels.Photo82
import com.merfilom.myphotostories.domain.models.photomodels.Photo83
import com.merfilom.myphotostories.domain.models.photomodels.Photo84
import com.merfilom.myphotostories.domain.models.photomodels.Photo85
import com.merfilom.myphotostories.domain.models.photomodels.Photo86
import com.merfilom.myphotostories.domain.models.photomodels.Photo87
import com.merfilom.myphotostories.domain.models.photomodels.Photo88
import com.merfilom.myphotostories.domain.models.photomodels.Photo89
import com.merfilom.myphotostories.domain.models.photomodels.Photo90
import com.merfilom.myphotostories.domain.models.photomodels.Photo91
import com.merfilom.myphotostories.domain.models.photomodels.Photo92
import com.merfilom.myphotostories.domain.models.photomodels.Photo93
import com.merfilom.myphotostories.domain.models.photomodels.Photo94
import com.merfilom.myphotostories.domain.models.photomodels.Photo95
import com.merfilom.myphotostories.domain.models.photomodels.Photo96
import com.merfilom.myphotostories.domain.models.photomodels.Photo97
import com.merfilom.myphotostories.domain.models.photomodels.Photo98
import com.merfilom.myphotostories.domain.models.photomodels.Photo99
import com.merfilom.myphotostories.domain.models.photomodels.Photo100


class Photo1ListMapper: PhotoMapperInterface<List<Photo1Entity>, List<Photo1>> {
    override fun mapFromEntity(type: List<Photo1Entity>): List<Photo1> {
        val listPhoto = ArrayList<Photo1>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo1(content = photoEntity.contentEntity, image = photoEntity.imageEntity,)) }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo1>): List<Photo1Entity> {
        val listPhotoEntity = ArrayList<Photo1Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo1Entity(contentEntity = photo.content, imageEntity = photo.image,)) }
        return listPhotoEntity.toList()
    }
}
class Photo2ListMapper: PhotoMapperInterface<List<Photo2Entity>, List<Photo2>> {
    override fun mapFromEntity(type: List<Photo2Entity>): List<Photo2> {
        val listPhoto = ArrayList<Photo2>()
        type.forEach { photoEntity -> listPhoto.add(Photo2(content = photoEntity.contentEntity, image = photoEntity.imageEntity,)) }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo2>): List<Photo2Entity> {
        val listPhotoEntity = ArrayList<Photo2Entity>()
        type.forEach { photo -> listPhotoEntity.add(Photo2Entity(contentEntity = photo.content, imageEntity = photo.image,)) }
        return listPhotoEntity.toList()
    }
}
class Photo3ListMapper: PhotoMapperInterface<List<Photo3Entity>, List<Photo3>> {
    override fun mapFromEntity(type: List<Photo3Entity>): List<Photo3> {
        val listPhoto = ArrayList<Photo3>()
        type.forEach { photoEntity -> listPhoto.add(Photo3(content = photoEntity.contentEntity, image = photoEntity.imageEntity,)) }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo3>): List<Photo3Entity> {
        val listPhotoEntity = ArrayList<Photo3Entity>()
        type.forEach { photo -> listPhotoEntity.add(Photo3Entity(contentEntity = photo.content, imageEntity = photo.image,)) }
        return listPhotoEntity.toList()
    }
}
class Photo4ListMapper: PhotoMapperInterface<List<Photo4Entity>, List<Photo4>> {
    override fun mapFromEntity(type: List<Photo4Entity>): List<Photo4> {
        val listPhoto = ArrayList<Photo4>()
        type.forEach { photoEntity -> listPhoto.add(Photo4(content = photoEntity.contentEntity, image = photoEntity.imageEntity,)) }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo4>): List<Photo4Entity> {
        val listPhotoEntity = ArrayList<Photo4Entity>()
        type.forEach { photo -> listPhotoEntity.add(Photo4Entity(contentEntity = photo.content, imageEntity = photo.image,)) }
        return listPhotoEntity.toList()
    }
}
class Photo5ListMapper: PhotoMapperInterface<List<Photo5Entity>, List<Photo5>> {
    override fun mapFromEntity(type: List<Photo5Entity>): List<Photo5> {
        val listPhoto = ArrayList<Photo5>()
        type.forEach { photoEntity -> listPhoto.add(Photo5(content = photoEntity.contentEntity, image = photoEntity.imageEntity,)) }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo5>): List<Photo5Entity> {
        val listPhotoEntity = ArrayList<Photo5Entity>()
        type.forEach { photo -> listPhotoEntity.add(Photo5Entity(contentEntity = photo.content, imageEntity = photo.image,)) }
        return listPhotoEntity.toList()
    }
}
class Photo6ListMapper: PhotoMapperInterface<List<Photo6Entity>, List<Photo6>> {
    override fun mapFromEntity(type: List<Photo6Entity>): List<Photo6> {
        val listPhoto = ArrayList<Photo6>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo6(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo6>): List<Photo6Entity> {
        val listPhotoEntity = ArrayList<Photo6Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo6Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo7ListMapper: PhotoMapperInterface<List<Photo7Entity>, List<Photo7>> {
    override fun mapFromEntity(type: List<Photo7Entity>): List<Photo7> {
        val listPhoto = ArrayList<Photo7>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo7(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo7>): List<Photo7Entity> {
        val listPhotoEntity = ArrayList<Photo7Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo7Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo8ListMapper: PhotoMapperInterface<List<Photo8Entity>, List<Photo8>> {
    override fun mapFromEntity(type: List<Photo8Entity>): List<Photo8> {
        val listPhoto = ArrayList<Photo8>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo8(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo8>): List<Photo8Entity> {
        val listPhotoEntity = ArrayList<Photo8Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo8Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo9ListMapper: PhotoMapperInterface<List<Photo9Entity>, List<Photo9>> {
    override fun mapFromEntity(type: List<Photo9Entity>): List<Photo9> {
        val listPhoto = ArrayList<Photo9>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo9(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo9>): List<Photo9Entity> {
        val listPhotoEntity = ArrayList<Photo9Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo9Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo10ListMapper: PhotoMapperInterface<List<Photo10Entity>, List<Photo10>> {
    override fun mapFromEntity(type: List<Photo10Entity>): List<Photo10> {
        val listPhoto = ArrayList<Photo10>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo10(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo10>): List<Photo10Entity> {
        val listPhotoEntity = ArrayList<Photo10Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo10Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo11ListMapper: PhotoMapperInterface<List<Photo11Entity>, List<Photo11>> {
    override fun mapFromEntity(type: List<Photo11Entity>): List<Photo11> {
        val listPhoto = ArrayList<Photo11>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo11(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo11>): List<Photo11Entity> {
        val listPhotoEntity = ArrayList<Photo11Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo11Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo12ListMapper: PhotoMapperInterface<List<Photo12Entity>, List<Photo12>> {
    override fun mapFromEntity(type: List<Photo12Entity>): List<Photo12> {
        val listPhoto = ArrayList<Photo12>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo12(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo12>): List<Photo12Entity> {
        val listPhotoEntity = ArrayList<Photo12Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo12Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo13ListMapper: PhotoMapperInterface<List<Photo13Entity>, List<Photo13>> {
    override fun mapFromEntity(type: List<Photo13Entity>): List<Photo13> {
        val listPhoto = ArrayList<Photo13>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo13(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo13>): List<Photo13Entity> {
        val listPhotoEntity = ArrayList<Photo13Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo13Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo14ListMapper: PhotoMapperInterface<List<Photo14Entity>, List<Photo14>> {
    override fun mapFromEntity(type: List<Photo14Entity>): List<Photo14> {
        val listPhoto = ArrayList<Photo14>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo14(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo14>): List<Photo14Entity> {
        val listPhotoEntity = ArrayList<Photo14Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo14Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo15ListMapper: PhotoMapperInterface<List<Photo15Entity>, List<Photo15>> {
    override fun mapFromEntity(type: List<Photo15Entity>): List<Photo15> {
        val listPhoto = ArrayList<Photo15>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo15(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo15>): List<Photo15Entity> {
        val listPhotoEntity = ArrayList<Photo15Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo15Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo16ListMapper: PhotoMapperInterface<List<Photo16Entity>, List<Photo16>> {
    override fun mapFromEntity(type: List<Photo16Entity>): List<Photo16> {
        val listPhoto = ArrayList<Photo16>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo16(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo16>): List<Photo16Entity> {
        val listPhotoEntity = ArrayList<Photo16Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo16Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo17ListMapper: PhotoMapperInterface<List<Photo17Entity>, List<Photo17>> {
    override fun mapFromEntity(type: List<Photo17Entity>): List<Photo17> {
        val listPhoto = ArrayList<Photo17>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo17(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo17>): List<Photo17Entity> {
        val listPhotoEntity = ArrayList<Photo17Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo17Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo18ListMapper: PhotoMapperInterface<List<Photo18Entity>, List<Photo18>> {
    override fun mapFromEntity(type: List<Photo18Entity>): List<Photo18> {
        val listPhoto = ArrayList<Photo18>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo18(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo18>): List<Photo18Entity> {
        val listPhotoEntity = ArrayList<Photo18Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo18Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo19ListMapper: PhotoMapperInterface<List<Photo19Entity>, List<Photo19>> {
    override fun mapFromEntity(type: List<Photo19Entity>): List<Photo19> {
        val listPhoto = ArrayList<Photo19>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo19(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo19>): List<Photo19Entity> {
        val listPhotoEntity = ArrayList<Photo19Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo19Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo20ListMapper: PhotoMapperInterface<List<Photo20Entity>, List<Photo20>> {
    override fun mapFromEntity(type: List<Photo20Entity>): List<Photo20> {
        val listPhoto = ArrayList<Photo20>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo20(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo20>): List<Photo20Entity> {
        val listPhotoEntity = ArrayList<Photo20Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo20Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo21ListMapper: PhotoMapperInterface<List<Photo21Entity>, List<Photo21>> {
    override fun mapFromEntity(type: List<Photo21Entity>): List<Photo21> {
        val listPhoto = ArrayList<Photo21>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo21(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo21>): List<Photo21Entity> {
        val listPhotoEntity = ArrayList<Photo21Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo21Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo22ListMapper: PhotoMapperInterface<List<Photo22Entity>, List<Photo22>> {
    override fun mapFromEntity(type: List<Photo22Entity>): List<Photo22> {
        val listPhoto = ArrayList<Photo22>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo22(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo22>): List<Photo22Entity> {
        val listPhotoEntity = ArrayList<Photo22Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo22Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo23ListMapper: PhotoMapperInterface<List<Photo23Entity>, List<Photo23>> {
    override fun mapFromEntity(type: List<Photo23Entity>): List<Photo23> {
        val listPhoto = ArrayList<Photo23>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo23(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo23>): List<Photo23Entity> {
        val listPhotoEntity = ArrayList<Photo23Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo23Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo24ListMapper: PhotoMapperInterface<List<Photo24Entity>, List<Photo24>> {
    override fun mapFromEntity(type: List<Photo24Entity>): List<Photo24> {
        val listPhoto = ArrayList<Photo24>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo24(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo24>): List<Photo24Entity> {
        val listPhotoEntity = ArrayList<Photo24Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo24Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo25ListMapper: PhotoMapperInterface<List<Photo25Entity>, List<Photo25>> {
    override fun mapFromEntity(type: List<Photo25Entity>): List<Photo25> {
        val listPhoto = ArrayList<Photo25>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo25(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo25>): List<Photo25Entity> {
        val listPhotoEntity = ArrayList<Photo25Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo25Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo26ListMapper: PhotoMapperInterface<List<Photo26Entity>, List<Photo26>> {
    override fun mapFromEntity(type: List<Photo26Entity>): List<Photo26> {
        val listPhoto = ArrayList<Photo26>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo26(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo26>): List<Photo26Entity> {
        val listPhotoEntity = ArrayList<Photo26Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo26Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo27ListMapper: PhotoMapperInterface<List<Photo27Entity>, List<Photo27>> {
    override fun mapFromEntity(type: List<Photo27Entity>): List<Photo27> {
        val listPhoto = ArrayList<Photo27>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo27(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo27>): List<Photo27Entity> {
        val listPhotoEntity = ArrayList<Photo27Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo27Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo28ListMapper: PhotoMapperInterface<List<Photo28Entity>, List<Photo28>> {
    override fun mapFromEntity(type: List<Photo28Entity>): List<Photo28> {
        val listPhoto = ArrayList<Photo28>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo28(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo28>): List<Photo28Entity> {
        val listPhotoEntity = ArrayList<Photo28Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo28Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo29ListMapper: PhotoMapperInterface<List<Photo29Entity>, List<Photo29>> {
    override fun mapFromEntity(type: List<Photo29Entity>): List<Photo29> {
        val listPhoto = ArrayList<Photo29>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo29(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo29>): List<Photo29Entity> {
        val listPhotoEntity = ArrayList<Photo29Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo29Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo30ListMapper: PhotoMapperInterface<List<Photo30Entity>, List<Photo30>> {
    override fun mapFromEntity(type: List<Photo30Entity>): List<Photo30> {
        val listPhoto = ArrayList<Photo30>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo30(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo30>): List<Photo30Entity> {
        val listPhotoEntity = ArrayList<Photo30Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo30Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo31ListMapper: PhotoMapperInterface<List<Photo31Entity>, List<Photo31>> {
    override fun mapFromEntity(type: List<Photo31Entity>): List<Photo31> {
        val listPhoto = ArrayList<Photo31>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo31(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo31>): List<Photo31Entity> {
        val listPhotoEntity = ArrayList<Photo31Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo31Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo32ListMapper: PhotoMapperInterface<List<Photo32Entity>, List<Photo32>> {
    override fun mapFromEntity(type: List<Photo32Entity>): List<Photo32> {
        val listPhoto = ArrayList<Photo32>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo32(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo32>): List<Photo32Entity> {
        val listPhotoEntity = ArrayList<Photo32Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo32Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo33ListMapper: PhotoMapperInterface<List<Photo33Entity>, List<Photo33>> {
    override fun mapFromEntity(type: List<Photo33Entity>): List<Photo33> {
        val listPhoto = ArrayList<Photo33>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo33(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo33>): List<Photo33Entity> {
        val listPhotoEntity = ArrayList<Photo33Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo33Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo34ListMapper: PhotoMapperInterface<List<Photo34Entity>, List<Photo34>> {
    override fun mapFromEntity(type: List<Photo34Entity>): List<Photo34> {
        val listPhoto = ArrayList<Photo34>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo34(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo34>): List<Photo34Entity> {
        val listPhotoEntity = ArrayList<Photo34Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo34Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo35ListMapper: PhotoMapperInterface<List<Photo35Entity>, List<Photo35>> {
    override fun mapFromEntity(type: List<Photo35Entity>): List<Photo35> {
        val listPhoto = ArrayList<Photo35>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo35(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo35>): List<Photo35Entity> {
        val listPhotoEntity = ArrayList<Photo35Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo35Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo36ListMapper: PhotoMapperInterface<List<Photo36Entity>, List<Photo36>> {
    override fun mapFromEntity(type: List<Photo36Entity>): List<Photo36> {
        val listPhoto = ArrayList<Photo36>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo36(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo36>): List<Photo36Entity> {
        val listPhotoEntity = ArrayList<Photo36Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo36Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo37ListMapper: PhotoMapperInterface<List<Photo37Entity>, List<Photo37>> {
    override fun mapFromEntity(type: List<Photo37Entity>): List<Photo37> {
        val listPhoto = ArrayList<Photo37>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo37(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo37>): List<Photo37Entity> {
        val listPhotoEntity = ArrayList<Photo37Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo37Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo38ListMapper: PhotoMapperInterface<List<Photo38Entity>, List<Photo38>> {
    override fun mapFromEntity(type: List<Photo38Entity>): List<Photo38> {
        val listPhoto = ArrayList<Photo38>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo38(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo38>): List<Photo38Entity> {
        val listPhotoEntity = ArrayList<Photo38Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo38Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo39ListMapper: PhotoMapperInterface<List<Photo39Entity>, List<Photo39>> {
    override fun mapFromEntity(type: List<Photo39Entity>): List<Photo39> {
        val listPhoto = ArrayList<Photo39>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo39(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo39>): List<Photo39Entity> {
        val listPhotoEntity = ArrayList<Photo39Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo39Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo40ListMapper: PhotoMapperInterface<List<Photo40Entity>, List<Photo40>> {
    override fun mapFromEntity(type: List<Photo40Entity>): List<Photo40> {
        val listPhoto = ArrayList<Photo40>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo40(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo40>): List<Photo40Entity> {
        val listPhotoEntity = ArrayList<Photo40Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo40Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo41ListMapper: PhotoMapperInterface<List<Photo41Entity>, List<Photo41>> {
    override fun mapFromEntity(type: List<Photo41Entity>): List<Photo41> {
        val listPhoto = ArrayList<Photo41>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo41(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo41>): List<Photo41Entity> {
        val listPhotoEntity = ArrayList<Photo41Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo41Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo42ListMapper: PhotoMapperInterface<List<Photo42Entity>, List<Photo42>> {
    override fun mapFromEntity(type: List<Photo42Entity>): List<Photo42> {
        val listPhoto = ArrayList<Photo42>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo42(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo42>): List<Photo42Entity> {
        val listPhotoEntity = ArrayList<Photo42Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo42Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo43ListMapper: PhotoMapperInterface<List<Photo43Entity>, List<Photo43>> {
    override fun mapFromEntity(type: List<Photo43Entity>): List<Photo43> {
        val listPhoto = ArrayList<Photo43>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo43(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo43>): List<Photo43Entity> {
        val listPhotoEntity = ArrayList<Photo43Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo43Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo44ListMapper: PhotoMapperInterface<List<Photo44Entity>, List<Photo44>> {
    override fun mapFromEntity(type: List<Photo44Entity>): List<Photo44> {
        val listPhoto = ArrayList<Photo44>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo44(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo44>): List<Photo44Entity> {
        val listPhotoEntity = ArrayList<Photo44Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo44Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo45ListMapper: PhotoMapperInterface<List<Photo45Entity>, List<Photo45>> {
    override fun mapFromEntity(type: List<Photo45Entity>): List<Photo45> {
        val listPhoto = ArrayList<Photo45>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo45(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo45>): List<Photo45Entity> {
        val listPhotoEntity = ArrayList<Photo45Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo45Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo46ListMapper: PhotoMapperInterface<List<Photo46Entity>, List<Photo46>> {
    override fun mapFromEntity(type: List<Photo46Entity>): List<Photo46> {
        val listPhoto = ArrayList<Photo46>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo46(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo46>): List<Photo46Entity> {
        val listPhotoEntity = ArrayList<Photo46Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo46Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo47ListMapper: PhotoMapperInterface<List<Photo47Entity>, List<Photo47>> {
    override fun mapFromEntity(type: List<Photo47Entity>): List<Photo47> {
        val listPhoto = ArrayList<Photo47>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo47(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo47>): List<Photo47Entity> {
        val listPhotoEntity = ArrayList<Photo47Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo47Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo48ListMapper: PhotoMapperInterface<List<Photo48Entity>, List<Photo48>> {
    override fun mapFromEntity(type: List<Photo48Entity>): List<Photo48> {
        val listPhoto = ArrayList<Photo48>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo48(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo48>): List<Photo48Entity> {
        val listPhotoEntity = ArrayList<Photo48Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo48Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo49ListMapper: PhotoMapperInterface<List<Photo49Entity>, List<Photo49>> {
    override fun mapFromEntity(type: List<Photo49Entity>): List<Photo49> {
        val listPhoto = ArrayList<Photo49>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo49(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo49>): List<Photo49Entity> {
        val listPhotoEntity = ArrayList<Photo49Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo49Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo50ListMapper: PhotoMapperInterface<List<Photo50Entity>, List<Photo50>> {
    override fun mapFromEntity(type: List<Photo50Entity>): List<Photo50> {
        val listPhoto = ArrayList<Photo50>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo50(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo50>): List<Photo50Entity> {
        val listPhotoEntity = ArrayList<Photo50Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo50Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo51ListMapper: PhotoMapperInterface<List<Photo51Entity>, List<Photo51>> {
    override fun mapFromEntity(type: List<Photo51Entity>): List<Photo51> {
        val listPhoto = ArrayList<Photo51>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo51(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo51>): List<Photo51Entity> {
        val listPhotoEntity = ArrayList<Photo51Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo51Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo52ListMapper: PhotoMapperInterface<List<Photo52Entity>, List<Photo52>> {
    override fun mapFromEntity(type: List<Photo52Entity>): List<Photo52> {
        val listPhoto = ArrayList<Photo52>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo52(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo52>): List<Photo52Entity> {
        val listPhotoEntity = ArrayList<Photo52Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo52Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo53ListMapper: PhotoMapperInterface<List<Photo53Entity>, List<Photo53>> {
    override fun mapFromEntity(type: List<Photo53Entity>): List<Photo53> {
        val listPhoto = ArrayList<Photo53>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo53(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo53>): List<Photo53Entity> {
        val listPhotoEntity = ArrayList<Photo53Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo53Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo54ListMapper: PhotoMapperInterface<List<Photo54Entity>, List<Photo54>> {
    override fun mapFromEntity(type: List<Photo54Entity>): List<Photo54> {
        val listPhoto = ArrayList<Photo54>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo54(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo54>): List<Photo54Entity> {
        val listPhotoEntity = ArrayList<Photo54Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo54Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo55ListMapper: PhotoMapperInterface<List<Photo55Entity>, List<Photo55>> {
    override fun mapFromEntity(type: List<Photo55Entity>): List<Photo55> {
        val listPhoto = ArrayList<Photo55>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo55(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo55>): List<Photo55Entity> {
        val listPhotoEntity = ArrayList<Photo55Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo55Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo56ListMapper: PhotoMapperInterface<List<Photo56Entity>, List<Photo56>> {
    override fun mapFromEntity(type: List<Photo56Entity>): List<Photo56> {
        val listPhoto = ArrayList<Photo56>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo56(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo56>): List<Photo56Entity> {
        val listPhotoEntity = ArrayList<Photo56Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo56Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo57ListMapper: PhotoMapperInterface<List<Photo57Entity>, List<Photo57>> {
    override fun mapFromEntity(type: List<Photo57Entity>): List<Photo57> {
        val listPhoto = ArrayList<Photo57>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo57(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo57>): List<Photo57Entity> {
        val listPhotoEntity = ArrayList<Photo57Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo57Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo58ListMapper: PhotoMapperInterface<List<Photo58Entity>, List<Photo58>> {
    override fun mapFromEntity(type: List<Photo58Entity>): List<Photo58> {
        val listPhoto = ArrayList<Photo58>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo58(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo58>): List<Photo58Entity> {
        val listPhotoEntity = ArrayList<Photo58Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo58Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo59ListMapper: PhotoMapperInterface<List<Photo59Entity>, List<Photo59>> {
    override fun mapFromEntity(type: List<Photo59Entity>): List<Photo59> {
        val listPhoto = ArrayList<Photo59>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo59(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo59>): List<Photo59Entity> {
        val listPhotoEntity = ArrayList<Photo59Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo59Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo60ListMapper: PhotoMapperInterface<List<Photo60Entity>, List<Photo60>> {
    override fun mapFromEntity(type: List<Photo60Entity>): List<Photo60> {
        val listPhoto = ArrayList<Photo60>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo60(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo60>): List<Photo60Entity> {
        val listPhotoEntity = ArrayList<Photo60Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo60Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo61ListMapper: PhotoMapperInterface<List<Photo61Entity>, List<Photo61>> {
    override fun mapFromEntity(type: List<Photo61Entity>): List<Photo61> {
        val listPhoto = ArrayList<Photo61>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo61(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo61>): List<Photo61Entity> {
        val listPhotoEntity = ArrayList<Photo61Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo61Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo62ListMapper: PhotoMapperInterface<List<Photo62Entity>, List<Photo62>> {
    override fun mapFromEntity(type: List<Photo62Entity>): List<Photo62> {
        val listPhoto = ArrayList<Photo62>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo62(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo62>): List<Photo62Entity> {
        val listPhotoEntity = ArrayList<Photo62Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo62Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo63ListMapper: PhotoMapperInterface<List<Photo63Entity>, List<Photo63>> {
    override fun mapFromEntity(type: List<Photo63Entity>): List<Photo63> {
        val listPhoto = ArrayList<Photo63>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo63(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo63>): List<Photo63Entity> {
        val listPhotoEntity = ArrayList<Photo63Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo63Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo64ListMapper: PhotoMapperInterface<List<Photo64Entity>, List<Photo64>> {
    override fun mapFromEntity(type: List<Photo64Entity>): List<Photo64> {
        val listPhoto = ArrayList<Photo64>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo64(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo64>): List<Photo64Entity> {
        val listPhotoEntity = ArrayList<Photo64Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo64Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo65ListMapper: PhotoMapperInterface<List<Photo65Entity>, List<Photo65>> {
    override fun mapFromEntity(type: List<Photo65Entity>): List<Photo65> {
        val listPhoto = ArrayList<Photo65>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo65(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo65>): List<Photo65Entity> {
        val listPhotoEntity = ArrayList<Photo65Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo65Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo66ListMapper: PhotoMapperInterface<List<Photo66Entity>, List<Photo66>> {
    override fun mapFromEntity(type: List<Photo66Entity>): List<Photo66> {
        val listPhoto = ArrayList<Photo66>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo66(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo66>): List<Photo66Entity> {
        val listPhotoEntity = ArrayList<Photo66Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo66Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo67ListMapper: PhotoMapperInterface<List<Photo67Entity>, List<Photo67>> {
    override fun mapFromEntity(type: List<Photo67Entity>): List<Photo67> {
        val listPhoto = ArrayList<Photo67>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo67(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo67>): List<Photo67Entity> {
        val listPhotoEntity = ArrayList<Photo67Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo67Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo68ListMapper: PhotoMapperInterface<List<Photo68Entity>, List<Photo68>> {
    override fun mapFromEntity(type: List<Photo68Entity>): List<Photo68> {
        val listPhoto = ArrayList<Photo68>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo68(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo68>): List<Photo68Entity> {
        val listPhotoEntity = ArrayList<Photo68Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo68Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo69ListMapper: PhotoMapperInterface<List<Photo69Entity>, List<Photo69>> {
    override fun mapFromEntity(type: List<Photo69Entity>): List<Photo69> {
        val listPhoto = ArrayList<Photo69>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo69(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo69>): List<Photo69Entity> {
        val listPhotoEntity = ArrayList<Photo69Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo69Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo70ListMapper: PhotoMapperInterface<List<Photo70Entity>, List<Photo70>> {
    override fun mapFromEntity(type: List<Photo70Entity>): List<Photo70> {
        val listPhoto = ArrayList<Photo70>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo70(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo70>): List<Photo70Entity> {
        val listPhotoEntity = ArrayList<Photo70Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo70Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo71ListMapper: PhotoMapperInterface<List<Photo71Entity>, List<Photo71>> {
    override fun mapFromEntity(type: List<Photo71Entity>): List<Photo71> {
        val listPhoto = ArrayList<Photo71>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo71(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo71>): List<Photo71Entity> {
        val listPhotoEntity = ArrayList<Photo71Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo71Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo72ListMapper: PhotoMapperInterface<List<Photo72Entity>, List<Photo72>> {
    override fun mapFromEntity(type: List<Photo72Entity>): List<Photo72> {
        val listPhoto = ArrayList<Photo72>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo72(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo72>): List<Photo72Entity> {
        val listPhotoEntity = ArrayList<Photo72Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo72Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo73ListMapper: PhotoMapperInterface<List<Photo73Entity>, List<Photo73>> {
    override fun mapFromEntity(type: List<Photo73Entity>): List<Photo73> {
        val listPhoto = ArrayList<Photo73>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo73(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo73>): List<Photo73Entity> {
        val listPhotoEntity = ArrayList<Photo73Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo73Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo74ListMapper: PhotoMapperInterface<List<Photo74Entity>, List<Photo74>> {
    override fun mapFromEntity(type: List<Photo74Entity>): List<Photo74> {
        val listPhoto = ArrayList<Photo74>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo74(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo74>): List<Photo74Entity> {
        val listPhotoEntity = ArrayList<Photo74Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo74Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo75ListMapper: PhotoMapperInterface<List<Photo75Entity>, List<Photo75>> {
    override fun mapFromEntity(type: List<Photo75Entity>): List<Photo75> {
        val listPhoto = ArrayList<Photo75>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo75(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo75>): List<Photo75Entity> {
        val listPhotoEntity = ArrayList<Photo75Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo75Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo76ListMapper: PhotoMapperInterface<List<Photo76Entity>, List<Photo76>> {
    override fun mapFromEntity(type: List<Photo76Entity>): List<Photo76> {
        val listPhoto = ArrayList<Photo76>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo76(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo76>): List<Photo76Entity> {
        val listPhotoEntity = ArrayList<Photo76Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo76Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo77ListMapper: PhotoMapperInterface<List<Photo77Entity>, List<Photo77>> {
    override fun mapFromEntity(type: List<Photo77Entity>): List<Photo77> {
        val listPhoto = ArrayList<Photo77>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo77(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo77>): List<Photo77Entity> {
        val listPhotoEntity = ArrayList<Photo77Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo77Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo78ListMapper: PhotoMapperInterface<List<Photo78Entity>, List<Photo78>> {
    override fun mapFromEntity(type: List<Photo78Entity>): List<Photo78> {
        val listPhoto = ArrayList<Photo78>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo78(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo78>): List<Photo78Entity> {
        val listPhotoEntity = ArrayList<Photo78Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo78Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo79ListMapper: PhotoMapperInterface<List<Photo79Entity>, List<Photo79>> {
    override fun mapFromEntity(type: List<Photo79Entity>): List<Photo79> {
        val listPhoto = ArrayList<Photo79>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo79(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo79>): List<Photo79Entity> {
        val listPhotoEntity = ArrayList<Photo79Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo79Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo80ListMapper: PhotoMapperInterface<List<Photo80Entity>, List<Photo80>> {
    override fun mapFromEntity(type: List<Photo80Entity>): List<Photo80> {
        val listPhoto = ArrayList<Photo80>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo80(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo80>): List<Photo80Entity> {
        val listPhotoEntity = ArrayList<Photo80Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo80Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo81ListMapper: PhotoMapperInterface<List<Photo81Entity>, List<Photo81>> {
    override fun mapFromEntity(type: List<Photo81Entity>): List<Photo81> {
        val listPhoto = ArrayList<Photo81>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo81(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo81>): List<Photo81Entity> {
        val listPhotoEntity = ArrayList<Photo81Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo81Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo82ListMapper: PhotoMapperInterface<List<Photo82Entity>, List<Photo82>> {
    override fun mapFromEntity(type: List<Photo82Entity>): List<Photo82> {
        val listPhoto = ArrayList<Photo82>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo82(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo82>): List<Photo82Entity> {
        val listPhotoEntity = ArrayList<Photo82Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo82Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo83ListMapper: PhotoMapperInterface<List<Photo83Entity>, List<Photo83>> {
    override fun mapFromEntity(type: List<Photo83Entity>): List<Photo83> {
        val listPhoto = ArrayList<Photo83>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo83(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo83>): List<Photo83Entity> {
        val listPhotoEntity = ArrayList<Photo83Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo83Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo84ListMapper: PhotoMapperInterface<List<Photo84Entity>, List<Photo84>> {
    override fun mapFromEntity(type: List<Photo84Entity>): List<Photo84> {
        val listPhoto = ArrayList<Photo84>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo84(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo84>): List<Photo84Entity> {
        val listPhotoEntity = ArrayList<Photo84Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo84Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo85ListMapper: PhotoMapperInterface<List<Photo85Entity>, List<Photo85>> {
    override fun mapFromEntity(type: List<Photo85Entity>): List<Photo85> {
        val listPhoto = ArrayList<Photo85>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo85(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo85>): List<Photo85Entity> {
        val listPhotoEntity = ArrayList<Photo85Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo85Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo86ListMapper: PhotoMapperInterface<List<Photo86Entity>, List<Photo86>> {
    override fun mapFromEntity(type: List<Photo86Entity>): List<Photo86> {
        val listPhoto = ArrayList<Photo86>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo86(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo86>): List<Photo86Entity> {
        val listPhotoEntity = ArrayList<Photo86Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo86Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo87ListMapper: PhotoMapperInterface<List<Photo87Entity>, List<Photo87>> {
    override fun mapFromEntity(type: List<Photo87Entity>): List<Photo87> {
        val listPhoto = ArrayList<Photo87>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo87(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo87>): List<Photo87Entity> {
        val listPhotoEntity = ArrayList<Photo87Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo87Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo88ListMapper: PhotoMapperInterface<List<Photo88Entity>, List<Photo88>> {
    override fun mapFromEntity(type: List<Photo88Entity>): List<Photo88> {
        val listPhoto = ArrayList<Photo88>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo88(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo88>): List<Photo88Entity> {
        val listPhotoEntity = ArrayList<Photo88Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo88Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo89ListMapper: PhotoMapperInterface<List<Photo89Entity>, List<Photo89>> {
    override fun mapFromEntity(type: List<Photo89Entity>): List<Photo89> {
        val listPhoto = ArrayList<Photo89>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo89(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo89>): List<Photo89Entity> {
        val listPhotoEntity = ArrayList<Photo89Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo89Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo90ListMapper: PhotoMapperInterface<List<Photo90Entity>, List<Photo90>> {
    override fun mapFromEntity(type: List<Photo90Entity>): List<Photo90> {
        val listPhoto = ArrayList<Photo90>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo90(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo90>): List<Photo90Entity> {
        val listPhotoEntity = ArrayList<Photo90Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo90Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo91ListMapper: PhotoMapperInterface<List<Photo91Entity>, List<Photo91>> {
    override fun mapFromEntity(type: List<Photo91Entity>): List<Photo91> {
        val listPhoto = ArrayList<Photo91>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo91(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo91>): List<Photo91Entity> {
        val listPhotoEntity = ArrayList<Photo91Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo91Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo92ListMapper: PhotoMapperInterface<List<Photo92Entity>, List<Photo92>> {
    override fun mapFromEntity(type: List<Photo92Entity>): List<Photo92> {
        val listPhoto = ArrayList<Photo92>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo92(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo92>): List<Photo92Entity> {
        val listPhotoEntity = ArrayList<Photo92Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo92Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo93ListMapper: PhotoMapperInterface<List<Photo93Entity>, List<Photo93>> {
    override fun mapFromEntity(type: List<Photo93Entity>): List<Photo93> {
        val listPhoto = ArrayList<Photo93>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo93(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo93>): List<Photo93Entity> {
        val listPhotoEntity = ArrayList<Photo93Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo93Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}
class Photo94ListMapper: PhotoMapperInterface<List<Photo94Entity>, List<Photo94>> {
    override fun mapFromEntity(type: List<Photo94Entity>): List<Photo94> {
        val listPhoto = ArrayList<Photo94>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo94(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo94>): List<Photo94Entity> {
        val listPhotoEntity = ArrayList<Photo94Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo94Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo95ListMapper: PhotoMapperInterface<List<Photo95Entity>, List<Photo95>> {
    override fun mapFromEntity(type: List<Photo95Entity>): List<Photo95> {
        val listPhoto = ArrayList<Photo95>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo95(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo95>): List<Photo95Entity> {
        val listPhotoEntity = ArrayList<Photo95Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo95Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo96ListMapper: PhotoMapperInterface<List<Photo96Entity>, List<Photo96>> {
    override fun mapFromEntity(type: List<Photo96Entity>): List<Photo96> {
        val listPhoto = ArrayList<Photo96>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo96(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo96>): List<Photo96Entity> {
        val listPhotoEntity = ArrayList<Photo96Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo96Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo97ListMapper: PhotoMapperInterface<List<Photo97Entity>, List<Photo97>> {
    override fun mapFromEntity(type: List<Photo97Entity>): List<Photo97> {
        val listPhoto = ArrayList<Photo97>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo97(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo97>): List<Photo97Entity> {
        val listPhotoEntity = ArrayList<Photo97Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo97Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo98ListMapper: PhotoMapperInterface<List<Photo98Entity>, List<Photo98>> {
    override fun mapFromEntity(type: List<Photo98Entity>): List<Photo98> {
        val listPhoto = ArrayList<Photo98>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo98(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo98>): List<Photo98Entity> {
        val listPhotoEntity = ArrayList<Photo98Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo98Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo99ListMapper: PhotoMapperInterface<List<Photo99Entity>, List<Photo99>> {
    override fun mapFromEntity(type: List<Photo99Entity>): List<Photo99> {
        val listPhoto = ArrayList<Photo99>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo99(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo99>): List<Photo99Entity> {
        val listPhotoEntity = ArrayList<Photo99Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo99Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}

class Photo100ListMapper: PhotoMapperInterface<List<Photo100Entity>, List<Photo100>> {
    override fun mapFromEntity(type: List<Photo100Entity>): List<Photo100> {
        val listPhoto = ArrayList<Photo100>()
        type.forEach { photoEntity ->
            listPhoto.add(Photo100(content = photoEntity.contentEntity, image = photoEntity.imageEntity))
        }
        return listPhoto.toList()
    }
    override fun mapToEntity(type: List<Photo100>): List<Photo100Entity> {
        val listPhotoEntity = ArrayList<Photo100Entity>()
        type.forEach { photo ->
            listPhotoEntity.add(Photo100Entity(contentEntity = photo.content, imageEntity = photo.image))
        }
        return listPhotoEntity.toList()
    }
}